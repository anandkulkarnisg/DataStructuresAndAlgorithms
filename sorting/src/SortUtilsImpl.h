#include<iostream>
#include<string>

// Add all template specilizations here for the sorting implementation. Then rebuild the library. This is the only way to incorporate the new types for templated libraries where
// All code need not be placed in the header file.

template class SortUtils<int>;
